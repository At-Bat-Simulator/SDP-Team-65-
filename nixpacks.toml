[phases.setup]
nixPkgs = ["python312", "nodejs_20"]

[phases.build]
cmds = [
  "cd frontend && npm install && npm run build",
  "cd api && pip install -r requirements.txt"
]

[start]
cmd = "cd api && gunicorn app:app --workers 1 --timeout 120"
